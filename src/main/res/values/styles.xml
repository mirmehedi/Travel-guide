<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Common styles layout attributes -->
    <style name="match_all">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    <style name="match_width">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="wrap_all">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- Text view style portrait -->
    <style name="text_view_portrait" parent="match_all">
        <item name="android:layout_margin">@dimen/margin_8dp</item>
        <item name="android:gravity">center_horizontal|center_vertical</item>
        <item name="android:textSize">@dimen/text_view_size_24sp</item>
    </style>

    <!-- Text view style list view-->
    <style name="text_list_view" parent="match_all">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginBottom">@dimen/margin_8dp</item>
        <item name="android:layout_marginLeft">@dimen/margin_8dp</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- Text view style detail -->
    <style name="text_view_detail" parent="match_width">
        <item name="android:gravity">center_horizontal|center_vertical</item>
        <item name="android:fontFamily">sans-serif-black</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <!-- Text view style info -->
    <style name="text_view_info" parent="match_width">
        <item name="android:layout_marginLeft">@dimen/margin_4dp</item>
        <item name="android:layout_marginRight">@dimen/margin_4dp</item>
        <item name="android:gravity">center_horizontal|center_vertical</item>
    </style>

</resources>
